function drawMap(definitionPrefix) {
    var newItemHtml = $('#' + definitionPrefix + '-location').html();

    var initMap = function(definitionPrefix) {
        // pass
    };
    return initMap;
}
